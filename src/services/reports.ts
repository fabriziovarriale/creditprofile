// Gestione report
export const reportService = {
  async generateReport(data: ReportData): Promise<Report> {
    // Generazione report
  },
  
  async sendReportByEmail(reportId: string, email: string): Promise<boolean> {
    // Invio report via email
  }
}; 